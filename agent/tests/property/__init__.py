"""The hypothesis properties.

Filled by the property-test tasks. Every property here runs under the profile
registered in `tests/conftest.py`, so each gets at least 100 generated examples
(Req 42.2) and prints a reproducible blob on failure (Req 42.4).
"""
